import styled from 'styled-components'
import { Colors } from '../../tokens'

export const RowField = styled.div`
  display: flex;
  color: ${Colors.WHITE};
  border-bottom: 1px solid ${Colors.TERRACOTTA};
  margin-bottom: 10px;
  font-size: 16px;
  line-height: 32px;
`
